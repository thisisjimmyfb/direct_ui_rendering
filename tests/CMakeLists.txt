# Tests — placeholder (full implementation: see PROGRESS.md "tests/CMakeLists.txt" task)
# This stub satisfies add_subdirectory(tests) so the top-level project configures cleanly.
# TODO: implement tests_unit and tests_render targets.
